// Write a function to sum two numbers

// Assumptions
// -need to take two numbers
// -return the sum of the inputs
// -should work for pos and neg numbers 
// -should work for both integers and floats 
// -non-numberic data types, try to convert to number & use, if fails return NaN

function sum(value1, value2) {
    return value1 + value2;
}

module.exports = sum;